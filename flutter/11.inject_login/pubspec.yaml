name: inject_login
description: A new Flutter project.

version: 1.0.0+1

environment:
  sdk: ">=2.1.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

  inject:
    path: vendor/inject

  bloc: ^2.0.0
  flutter_bloc: ^2.1.1
  meta: ^1.1.6
  equatable: ^1.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter

  inject_generator:
    path: vendor/inject_generator

  build_runner: ^1.0.0
  pedantic: ^1.7.0

flutter:
  uses-material-design: true
