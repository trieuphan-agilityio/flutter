name: app
description: A new Flutter project.

publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ">=2.7.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^0.1.3

  video_call_platform_interface:
    path: ../video_call_platform_interface

  twilio_video_call:
    path: ../twilio_video_call

  # The plugin has reached a stable API.
  # They guarantee that version 1.0.0 will be backward compatible with 0.5.y+z.
  # Use below dependency constraint to allow a smoother ecosystem migration.
  shared_preferences: ">=0.5.7+3 <2.0.0"

  provider: 4.1.3
  animations: 1.1.1
  meta: 1.1.8


dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
