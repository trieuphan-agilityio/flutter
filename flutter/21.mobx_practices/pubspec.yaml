name: mobx_practices
description: A new Flutter project.
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ">=2.7.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^0.1.3
  mobx: ^1.0.0
  flutter_mobx: ^1.0.0

  connectivity: any
  validators: any

dev_dependencies:
  flutter_test:
    sdk: flutter

  build_runner: ^1.7.2
  mobx_codegen: ^1.0.0

flutter:
  uses-material-design: true
